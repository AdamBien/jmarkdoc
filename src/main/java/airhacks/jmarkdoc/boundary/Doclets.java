package airhacks.jmarkdoc.boundary;

/**
 * Package placeholder and home for shared Doclet constants.
 *
 * <p>This type exists so the {@code com.airhacks.jmarkdoc} package is non-empty
 * and the module layout compiles cleanly. It will hold the Doclet name and
 * other shared constants as the implementation grows.
 */
public final class Doclets {

    /** The Doclet name returned by {@code Doclet.getName()}. */
    public static final String DOCLET_NAME = "MarkdownDoclet";

    private Doclets() {
    }
}
